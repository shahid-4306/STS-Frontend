export const environment = {
  production: false,
  apiUrl: 'https://sts-backend-three.vercel.app/api',
  // Origin used to resolve relative asset URLs returned by the API (e.g. saved invoice images
  // served from /uploads/...). In dev this is the backend's own origin.
  assetsOrigin: 'https://sts-backend-three.vercel.app',
};
