export const environment = {
  production: true,
  apiUrl: '/api',
  // In production the app is served from the same origin as the API (reverse-proxied),
  // so relative asset URLs need no origin prefix.
  assetsOrigin: '',
};
